language: java

matrix:
    include:
        - os: linux
          jdk: openjdk8
          env: PUSHY_SSL_PROVIDER=jdk

        - os: linux
          jdk: openjdk8

        - os: osx
          osx_image: xcode8.3
          env: PUSHY_SSL_PROVIDER=jdk

        - os: osx
          osx_image: xcode8.3
